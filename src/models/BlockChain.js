
class BlockChain {

/* Variables */
  var blockChain;
/* Métodos */
  BlockChain () {
  }

/* Getter y setter*/

//1. hash
  getBlockChain() {
    return this.blockChain;
  }

  setBlockChain(hash) {
    this.blockChain = blockChain;
  }
}

module.exports = BlockChain;
